import sys
import os
import types
import traceback
import subprocess

def line_prepender():
    with filename as f:
        content = f.read()
        f.seek(0, 0)
        f.write(line.rstrip('\r\n') + '\n' + content)

cmd1=['exec ./utils/launch.sh --vnc 10.10.0.72:5900']
proc = subprocess.Popen(cmd1,shell=True,preexec_fn=os.setsid)
f = open("PID.txt", "a+")
f.write(str(proc.pid) + '\n')
f.close()
print proc.pid
#proc.communicate()
